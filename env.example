DISCORD_TOKEN=
DISCORD_CLIENT_ID=
# Optional (recommended for dev): register commands only in one server for instant updates
DISCORD_GUILD_ID=

# Postgres connection (for docker-compose defaults)
DATABASE_URL=postgres://postgres:postgres@db:5432/drafter
